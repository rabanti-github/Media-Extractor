<Window x:Class="PortableInstaller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Portable Installer" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Portable Setup" FontSize="18" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Text="Please choose installation folder:" Margin="0,10,0,0"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,0">
            <TextBox Name="PathTextBox" Width="250" />
            <Button Content="Browse..." Margin="5,0,0,0" Click="Browse_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="3" VerticalAlignment="Bottom">
            <ProgressBar Name="InstallProgressBar" Height="20" Minimum="0" Maximum="100"/>
            <StatusBar Margin="0,5,0,0">
                <TextBlock Name="StatusText" Text="Ready"/>
            </StatusBar>
        </StackPanel>
    </Grid>
</Window>
